http:
  routers:
    api:
      entryPoints:
        - web
      rule: PathPrefix(`/api`) || PathPrefix(`/docs`) || PathPrefix(`/redoc`) || Path(`/openapi.json`)
      service: api

  services:
    api:
      loadBalancer:
        servers:
          - url: http://api:8000
